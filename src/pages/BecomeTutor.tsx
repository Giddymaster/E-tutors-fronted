import React from 'react'
import { Container, Typography } from '@mui/material'

export default function BecomeTutor() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Become a Tutor</Typography>
      <Typography sx={{ mt: 2 }}>Placeholder page for tutor signup and profile creation.</Typography>
    </Container>
  )
}
